pre-push:
  jobs:
    - name: lint
      tags:
        - frontend
      root: "/web"
      run: pnpm run lint
    - name: format
      tags:
        - frontend
      root: "/web"
      run: pnpm run format
