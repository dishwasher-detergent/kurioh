pre-commit:
  jobs:
    - name: lint
      root: "web"
      glob: "**/*.{js,ts,jsx,tsx}"
      run: pnpm run lint

    - name: format
      root: "web"
      glob: "**/*.{js,ts,jsx,tsx,html}"
      run: pnpm run format
